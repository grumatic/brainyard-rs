# by-rs Contract Fixtures

Commit-ready compatibility fixtures for the Rust port live here. Generated or
hand-written fixtures in this directory are source artifacts; local design notes
under `docs/` may exist but must be kept out of commits unless explicitly allowed.
